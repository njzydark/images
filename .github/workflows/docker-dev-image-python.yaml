name: Build Dev Image Python Image
on:
  workflow_run:
    workflows: ["Build Dev Image Nix Image"]
    types:
      - completed
  push:
    branches:
      - main
    paths:
      - 'dev-images/python/Dockerfile'
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/reusable-docker-image.yaml
    secrets: inherit
    with:
      image_name: dev-image
      image_tag: python
      cache_scope: dev-image-python
      context_path: dev-images/python
      dockerfile: Dockerfile
