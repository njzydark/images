name: Build Dev Image Rust Image
on:
  workflow_run:
    workflows: ["Build Dev Image Nix Image"]
    types:
      - completed
  push:
    branches:
      - main
    paths:
      - 'dev-images/rust/Dockerfile'
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/reusable-docker-image.yaml
    secrets: inherit
    with:
      image_name: dev-image
      image_tag: rust
      cache_scope: dev-image-rust
      context_path: dev-images/rust
      dockerfile: Dockerfile
