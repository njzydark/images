name: Build Dev Image Full Image
on:
  workflow_run:
    workflows: ["Build Dev Image Nix Image"]
    types:
      - completed
  push:
    branches:
      - main
    paths:
      - 'dev-images/full/Dockerfile'
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/reusable-docker-image.yaml
    secrets: inherit
    with:
      image_name: dev-image
      image_tag: full
      cache_scope: dev-image-full
      context_path: dev-images/full
      dockerfile: Dockerfile
